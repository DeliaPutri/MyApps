<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApps">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RS"></activity>
        <activity android:name=".RSAwalBross"></activity>

        <activity android:name=".POL"></activity>
        <activity android:name=".PoldaRiau"></activity>

        <activity android:name=".SEKOLAH"></activity>
        <activity android:name=".SMAN1Pekanbaru"></activity>

        <activity android:name=".SM"></activity>
        <activity android:name=".MalSKA"></activity>
    </application>

</manifest>